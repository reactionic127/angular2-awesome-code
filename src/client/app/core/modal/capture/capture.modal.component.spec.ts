import {
  async
} from '@angular/core/testing';

export function main() {
  describe('Capture modal component', () => {

    beforeEach(async(() => {

      // TestBed.configureTestingModule({
      //           ]
      // }).compileComponents();
    }));

    it('loadData function should work',
      async(() => {
        expect(true).toEqual(true);
      }));
  });
}
